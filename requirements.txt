# requirements.txt for the "MLenv" environment
#
# This repo's training code only needs a small subset of your full conda env.
#
# NOTE on PyTorch CUDA wheels:
# Your environment shows:
#   torch==2.9.1+cu130, torchvision==0.24.1+cu130, torchaudio==2.9.1+cu130
# These "local version" tags (+cu130) typically require the PyTorch wheel index.
# If pip cannot find these versions, either:
#   1) remove the "+cu130" pins and install the CUDA build via conda, OR
#   2) use the matching PyTorch wheel index URL for your CUDA build.

--extra-index-url https://download.pytorch.org/whl/cu130

torch==2.9.1+cu130
torchvision==0.24.1+cu130
torchaudio==2.9.1+cu130

# Used by the dataset/transforms and many ML workflows
numpy==2.3.5

# Optional: enables OpenCV fallback video decoding in dataset.py
opencv-python>=4.8

# Optional (recommended): TorchCodec video decoding (torchvision.io.read_video is deprecated)
# For torch==2.9.*, torchcodec 0.9 is the compatible stable series.
torchcodec>=0.9,<0.10
